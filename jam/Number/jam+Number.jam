with( $jam )
$define
(   '$Number'
,   $Class( function( klass, proto ){
    
        klass.create=
        function( data ){
            var obj= new klass
            obj.$= Number( data )
            return obj
        }
        
        proto.valueOf=
        function( ){
            return this.$
        }

    })
)
